# Workflow

The first workflow proves the observable run contract:

```text
idea.md -> run folder -> fake pipeline -> artifacts -> reports -> logs -> final status
```

Every step writes inspectable files and leaves the CLI runnable and testable.
